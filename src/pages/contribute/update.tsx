    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();W    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();📞    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();🕐    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();🔧    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();🏷    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();️    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();📍    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();🔒    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();✏    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();️    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();@    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();@    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();%    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();%    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();%    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();K    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();—    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();—    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();—    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();+    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();📧    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Q    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();•    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();W    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();─    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();─    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();─    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();─    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();📍    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();%    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();%    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();W    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();✓    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();📡    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();G    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();!    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();📍    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();—    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();✓    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();W    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();H    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();🔧    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();🏍    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();️    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();⚙    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();️    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();V    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();W    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();B    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();X    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();L    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();&    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();O    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();F    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();_    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();|    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();M    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();W    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();A    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();Y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();,    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();3    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();D    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();#    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();7    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();*    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();I    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();R    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();E    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();j    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();(    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();6    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();C    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();z    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();8    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();T    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();[    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();1    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();]    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();P    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();"    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();N    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();=    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();k    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();$    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();y    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();4    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();x    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();h    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();w    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();2    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();0    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();s    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();c    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();-    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();9    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();5    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();`    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();{    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();l    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();?    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();g    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();.    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();:    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();S    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();U    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();p    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();a    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();e    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();'    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();b    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();u    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();t    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();n    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();f    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();o    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();r    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();m    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();<    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();/    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();d    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();i    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();v    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();>    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();     const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();)    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();;    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();}    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();
    const checkAuth = async (attempt = 0) => {
      const { data: { session } } = await supabase.auth.getSession();
      if (session?.user) {
        setUser(session.user);
        setAuthLoading(false);
      } else if (attempt < 5) {
        setTimeout(() => checkAuth(attempt + 1), 300);
      } else {
        router.replace('/auth/signin?redirect=/contribute/update');
      }
    };
    checkAuth();